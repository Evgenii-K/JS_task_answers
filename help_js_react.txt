//	Собеседование:
// Изменяемость const
// Замкнутые функции
// Типы данных NULL и undefined
// Является ли массив отдельным типом данных? -  Массив это частный случай объектов (ключи заданны по умолчанию)
// Чем отличается префиксная от постфиксной формы (оператор ++ -- ставится перед или после переменной)
// Тернарный оператор (называется так по тому что используется три оператора). Бинарный - это "+" "-" и тд. Есть еще унарный, например + перед prompt.
// Замыкание функции


-- Если вывести console.log( typeof(NULL) ); значение будет object - это обшеизвестная ошибка

-- Метод ${}

-- Унарный плюс - это плюс который ставится перед чем то что хотим использовать как число 

-- Три знака равенства - это строгое равенство

-- Оператор отрицания - ! перед переменной

-- Оператор continue - исключает выполнение действия в цикле при определённом условии